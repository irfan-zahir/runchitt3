import { FC, PropsWithChildren } from "react";
const Dashboard:FC = ({...props})=>{
    return (
        <div className="min-h-full dashboard">
           dashboard
        </div>
    )
}

export default Dashboard